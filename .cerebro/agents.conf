# What each agent runs on: which tool, which model, how hard it thinks. Written by
# .claude/cerebro/scripts/install and edited by hand; .claude/cerebro/agents.conf.example is the shape.
# The most specific line wins whole: the agent's own name, then its role, then `default'.

default       tool=claude  model=opus  effort=medium
implementer   tool=claude  model=opus  effort=low

# Every other role runs on the default line. Uncomment one to give that role its own settings.
#ux            tool=claude  model=opus  effort=medium
#build-design  tool=claude  model=opus  effort=medium
#orchestrator  tool=claude  model=opus  effort=medium
#verifier      tool=claude  model=opus  effort=medium
#reviewer      tool=claude  model=opus  effort=medium
#user-feedback tool=claude  model=opus  effort=medium
#architect     tool=claude  model=opus  effort=medium
